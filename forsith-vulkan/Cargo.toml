[package]
name = "forsith-vulkan"
version = "0.1.0"
edition = "2021"

[dependencies]
bindings = { path = "../forsith-bindings", package = "forsith-bindings" }
shared = { path = "../forsith-shared", package = "forsith-shared" }
